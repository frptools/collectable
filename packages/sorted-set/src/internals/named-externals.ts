export {
  RedBlackTree,
  empty as emptyTree,
  thaw as thawTree,
  freeze as freezeTree,
  isThawed as isTreeThawed,
  clone as cloneTree,
  update as updateTreeEntry,
  set as setTreeValue,
  remove as removeTreeValue,
  updateTree
} from '@collectable/red-black-tree';

export {
  Map,
  empty as emptyMap,
  set as setMapEntry,
  thaw as thawMap,
  freeze as freezeMap,
  isThawed as isMapThawed,
  clone as cloneMap,
  updateMap,
  update as updateMapEntry
} from '@collectable/map';
